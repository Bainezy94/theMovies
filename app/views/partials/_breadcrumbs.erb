<nav>
    <div class="nav-wrapper  blue-grey">
        <div class="col s12">
            <% nav.each do |n| %>
                <%= link_to n[:title], n[:url], class: "breadcrumb" %>
            <% end %>
        </div>
    </div>
</nav>